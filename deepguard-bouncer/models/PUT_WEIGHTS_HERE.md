This folder is intentionally empty in the delivered project.

After you finish the Colab notebook, download `deepguard_bouncer.pth`
and place it in this exact folder, so the path is:

    deepguard-bouncer/models/deepguard_bouncer.pth

The FastAPI backend (`app/main.py`) looks for the file at exactly that
path. See README.md, Part 4, for the exact command.
