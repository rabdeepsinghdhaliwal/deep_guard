# Deep-Guard — test-only dependencies.
#
# Kept separate from requirements.txt on purpose: running the actual
# server never needs these, only running the test suite does.
#
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Run with:     pytest -v   (from this app/ directory)

pytest>=8.0

# httpx is what FastAPI's TestClient uses under the hood to make
# requests against the app in-process, with no real server/socket
# needed.
httpx>=0.27

# Only for tools/build_concept_labeler.py, the one-time build of the
# registry's CLIP "namer" (see models/PUT_WEIGHTS_HERE.md). The server
# itself loads the exported file with plain torch and never imports it.
open_clip_torch>=3.0
